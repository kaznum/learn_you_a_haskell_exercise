import Control.Monad

main = do
     input <- getLine
     when (input == "SWARDFISH") $ do
          putStrLn input

-- main = do
--      input <- getLine
--      if (input == "SWARDFISH")
--         then putStrLn input
--         else return ()

--- sequence
